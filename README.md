# PBL1-2

## 📁 任务一：手部抓握运动神经活动分析（Task 1）

本部分主要完成了对 `single-movement` 数据中左手抓握和右手抓握任务的事件相关频谱扰动（ERSP）分析。按照作业要求，比较了**抓握运动尝试阶段**相对于**静息 Rest 阶段**的硬膜外皮质脑电（eECoG）频谱变化，并输出相应的时频图和频率响应曲线。

### 文件说明

* **`task1_ersp.py`**：任务一核心代码脚本。用于读取 `single-movement` 数据，提取左手/右手抓握阶段与对应 Rest 阶段，计算 Movement / Rest 的 ERSP，并绘制时频图与频率曲线图。

* **`images/Fig_1c_Left_Grasp_vs_Rest_PDFRequirement.png`**：左手抓握阶段相对于 Rest 阶段的 8 通道 ERSP 时频图。该图展示了左手尝试抓握时，右半球 eECoG 中低频 beta 能量下降和 high gamma 能量增强的时间-频率变化。

* **`images/Fig_1d_Left_Right_vs_Rest_PDFRequirement.png`**：左手抓握与右手抓握在代表性通道 ch3 上的 ERSP 频率曲线对比图。该图展示了对侧左手抓握与同侧右手抓握在不同频段上的响应差异，尤其是 high gamma 频段的能量增强差异。

* **`任务1-分析报告.md`**：任务一分析说明文档。主要解释本任务的处理方法、两张结果图的含义，以及这些结果对后续连续抓握解码、多手势分类和海报展示的提示作用。

### 结果概述

任务一结果显示，左手尝试抓握阶段相对于 Rest 阶段，在右半球 eECoG 中表现出明显的运动相关频谱变化：

* beta 频段能量下降，表现为事件相关去同步化（ERD）；
* high gamma 频段能量增强，表现为事件相关同步化（ERS）；
* 左手抓握作为右半球电极的对侧运动，其 high gamma 响应强于右手抓握。

因此，后续任务中可以重点考虑使用 **50–100 Hz 或 50–150 Hz high gamma 能量** 作为连续解码和手势分类的重要特征。



##  📁任务四文件说明

本文件夹用于整理任务四“多手势运动连续神经解码”的代码、结果图片和说明文档，主要供后续撰写报告和制作海报的同学使用。

### 1. `images/`

该文件夹中存放的是我初步筛选后认为**有可能放到海报里的图片**，包括方法流程图、代表性混淆矩阵、连续解码示例图、Fig.3a 风格轨迹图等。

这些图片不是全部结果，而是从结果中挑出来的较适合展示的图。后续做海报时可以优先从这个文件夹中选择图片，如果觉得某张图不够合适，也可以到完整结果文件夹中重新挑选。

---

### 2. `全部结果_results_task4_v2/`

该文件夹中存放的是任务四 v2 代码运行后输出的**全部结果文件**。

其中包括：

* 7 类连续分类混淆矩阵；
* Rest/Open vs Movement 二分类结果；
* Movement-only 6 手势分类结果；
* 多个测试 trial 的连续解码图；
* 每个代表性 trial 的预测结果 CSV；
* 可用于后续 Fig.3a 风格绘图或 Omnihand 可视化的中间结果。

如果做海报或报告时觉得 `images/` 里的图片不够好，可以在这个文件夹里继续选择其他 trial 的结果图。

---

### 3. 代码文件

代码文件用于完成任务四的数据读取、特征提取、模型训练、测试评估和结果可视化。

主要包括：

* `task4_continuous_gesture_decode_v2_two_stage.py`
  任务四最终主代码。该代码使用 v2 两阶段伪在线连续解码方法：先判断 Rest/Open vs Movement，再在 Movement 阶段识别 6 种具体手势，并输出混淆矩阵、连续解码图和预测结果 CSV。

* `task4_plot_fig3a_style_v2_autofind.py`
  Fig.3a 风格绘图代码。该代码读取 v2 输出的 prediction CSV，将模型输出的手势概率映射到五根手指的弯曲模板，生成类似作业 PDF 中 Fig.3a 的连续手指轨迹图。

如需重新运行任务四结果，建议先运行 v2 主代码，再运行 Fig.3a 风格绘图代码。

---

### 4. Markdown 说明文件

Markdown 文件用于解释任务四的整体思路、代码输出结果、图像含义和海报展示建议。

主要内容包括：

* 任务四到底要做什么；
* hand-gesture 数据的含义；
* v1 / v2 / v3 不同方案的比较；
* 为什么最终选择 v2 作为主结果；
* 各类混淆矩阵和连续解码图应该如何解读；
* 哪些图片推荐放到海报中；
* 当前结果的局限性和后续改进方向。

后续做海报的同学可以优先阅读 Markdown 说明文件，再从 `images/` 或 `全部结果_results_task4_v2/` 中选择合适的图片进行展示。
